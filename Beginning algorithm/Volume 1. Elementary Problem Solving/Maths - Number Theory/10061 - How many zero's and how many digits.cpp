#include <iostream>
#include <cmath>

using namespace std;

int main ()
{
	int num , base;
	while ( cin >> num >> base )
	{
		int zero = 0 , digitals = 0;
		digitals = log ( sqrt ( 2.0 * num * 
	}

	system ( "pause" );
	return 0;
}

/* 
	log ( sqrt ( 2.0 * PI * num ) ) / log ( base ) + n * log ( n / e ) / log ( base )
*/