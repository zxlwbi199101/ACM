#include <iostream>
#include <string>
#include <stack>
#include <queue>

using namespace std;

int main()
{
	int count;
	cin >> count;
	while (count--)
	{
		string order;
		cin >> order;

		stack<char> stk;
		queue<char> que;
		for (int i = 0; i < order.size(); i++)
		{

		}
	}

	return 0;
}
